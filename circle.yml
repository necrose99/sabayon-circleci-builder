machine:
  services:
    - docker
dependencies:
  cache_directories:
    - ~/.bosonstate
  override:
    - if [ -n "$BUILD_ARGS" ]; then docker pull sabayon/builder-amd64; fi
test:
  override:
    - ./scripts/test.sh
deployment:
  production:
    branch: master
    commands:
      - ./scripts/deploy.sh
